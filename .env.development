# Left unset on purpose — CRA's "proxy" field (package.json) + apiConfig.js's empty
# default handle local dev without needing an absolute URL here. Only set
# REACT_APP_API_BASE_URL if you need to point local dev at a different backend.